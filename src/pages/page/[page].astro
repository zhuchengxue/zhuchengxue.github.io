---
export function getStaticPaths() {
  return Array.from({ length: 13 }, (_, index) => ({
    params: { page: String(index + 2) }
  }));
}

return Astro.redirect(`${import.meta.env.BASE_URL}articles/`, 301);
---
